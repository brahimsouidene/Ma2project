{% extends 'base.html.twig' %}

{% block body %}
    <h1>Client creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
       <input type="submit" value="Suivant" onClick=" {{ path('colis_index') }}"/>
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('client_index') }}">Back to the list</a>
            
        </li>
    </ul>
{% endblock %}
